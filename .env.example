# OpenClaw workspace path — markdown files are the data layer
WORKSPACE_PATH=/Users/data/.openclaw/workspace

# Server port
PORT=3001

# Dashboard passphrase (change this before deploying!)
PASSPHRASE=enterprise
